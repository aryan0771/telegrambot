version: '3.8'

services:
  mirror-bot:
    build: .
    container_name: telegram-mirror-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
